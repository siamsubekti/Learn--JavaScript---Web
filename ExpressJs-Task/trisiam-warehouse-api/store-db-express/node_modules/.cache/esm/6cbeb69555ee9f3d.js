let getRepository,Tenant;_55e‍.x([["default",()=>TenantService]]);_55e‍.w("typeorm",[["getRepository",["getRepository"],function(v){getRepository=v}]]);_55e‍.w("../models/tenant.model",[["default",["Tenant"],function(v){Tenant=v}]]);


               class TenantService {
    tenantRepository() {
        return getRepository(Tenant);
    }

    findAll() {
        return this.tenantRepository().find();
    }

    findOne(id) {
        return this.tenantRepository().findOne(id);
    }

    async create(tenantData) {
        const tenant = Object.assign(new Tenant(), tenantData);
        return await this.tenantRepository().save(tenant);
    }

    update(tenantData) {
        return this.tenantRepository().save(tenantData);
    }

    delete(id) {
        return this.tenantRepository().delete(id);
    }
}