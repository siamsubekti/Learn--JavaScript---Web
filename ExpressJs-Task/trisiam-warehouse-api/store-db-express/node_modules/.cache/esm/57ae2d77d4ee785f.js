let dotenv;_d4e‍.x([["default",()=>_d4e‍.o]]);_d4e‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);

function configure() {
    dotenv.config();

    if(!process.env.APP_NAME) {
        console.log(`required environment variable or environment variable 
        are unavailable or may incorrect`);
        process.exit(1);
    }
}

_d4e‍.d(configure);