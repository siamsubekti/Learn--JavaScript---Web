let getRepository,Warehouse;_710‍.x([["default",()=>WarehouseService]]);_710‍.w("typeorm",[["getRepository",["getRepository"],function(v){getRepository=v}]]);_710‍.w("../models/warehouse.model",[["default",["Warehouse"],function(v){Warehouse=v}]]);


               class WarehouseService {
    warehouseRepository() {
        return getRepository(Warehouse);
    }

    findAll() {
        return this.warehouseRepository().find();
    }

    async create(warehouseData) {
        const warehouse = Object.assign(new warehouse(), warehouseData);
        return await this.warehouseRepository().save(warehouse);
    }

    update(warehouseData) {
        return this.warehouseRepository().save(warehouseData);
    }
}