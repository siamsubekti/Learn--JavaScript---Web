class Manifest {
    constructor (id, storage_id, item_id, amount, createAt, updateAt) {
        this.id = id;
        this.storage_id = storage_id;
        this.item_id = item_id;
        this.amount = amount;
        this.createAt = createAt;
        this.updateAt = updateAt;
    }
}

export default Manifest;