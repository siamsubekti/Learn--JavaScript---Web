class Item {
    constructor (id, name, createAt, updateAt) {
        this.id = id;
        this.name = name;
        this.createAt = createAt;
        this.updateAt = updateAt;
    }
}

export default Item;