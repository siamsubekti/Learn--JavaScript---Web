let dotenv;_266‍.x([["default",()=>_266‍.o]]);_266‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);

function configure(){
    dotenv.config();

    if(!process.env.APP_NAME){
        console.error(`required environment variable or environment variable are unavailable or may incorrect`);
        process.exit(1);
    }
}

_266‍.d(configure);