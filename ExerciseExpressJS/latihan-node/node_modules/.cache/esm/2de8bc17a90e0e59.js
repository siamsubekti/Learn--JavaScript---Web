let getRepository,Users;_a90‍.x([["default",()=>UserService]]);_a90‍.w("typeorm",[["getRepository",["getRepository"],function(v){getRepository=v}]]);_a90‍.w("../models/user.model",[["default",["Users"],function(v){Users=v}]]);


               class UserService{
    userRepository(){
        return getRepository(Users);
    }

    findAll(){
        return this.userRepository().find();
    }

    findOne(id){
        return this.userRepository().findOne(id);
    }

    async create(userData){
        const user = Object.assign(new Users(), userData);
        return await this.userRepository().save(user);
    }

    update(userData){
        return this.userRepository().save(userData);
    }

    delete(id){
        return this.userRepository().delete(id);
    }
}