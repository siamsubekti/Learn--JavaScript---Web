class Post {
    constructor(id, title, date, createdAt, updatedAt) {
        this.id = id;
        this.title = title;
        this.date = date;
        this.createdAt = createdAt;
        this.updatedAt = updatedAt;
    }
}

export default Post;