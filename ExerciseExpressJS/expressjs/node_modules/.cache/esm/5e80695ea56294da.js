let repository,Post;_a56‍.x([["default",()=>PostService]]);_a56‍.w("typeorm",[["getRepository",["repository"],function(v){repository=v}]]);_a56‍.w("../models/post.model",[["default",["Post"],function(v){Post=v}]]);


               class PostService {
    postRepository() {
        return repository(Post);
    }

    findAll() {
        return this.postRepository().find({ relations: ['author']});
    }

    findOne(){
        return this.postRepository().findOne(id);
    }

    create(postDate) {
        return this.postRepository().save(postDate);
    }

    update(postDate) {
        
    }

    delete(id) {
        return this.postRepository().delete(id);
    }
}