let dotenv;_564‍.x([["default",()=>_564‍.o]]);_564‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);

function configure() {
    dotenv.config();

    if(!process.env.APP_NAME){
        console.error("Required environment variables are unavailable or may incorrectly loaded");
        process.exit(1);
    }
}

_564‍.d(configure);